{
  "name": "monorepo",
  "version": "1.0.0",
  "description": "a monorepo of components",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "olliew",
  "license": "ISC",
  "devDependencies": {
    "@springernature/global-package-manager": "^2.2.0"
  }
}
